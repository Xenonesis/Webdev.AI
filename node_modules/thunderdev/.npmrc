# Force npm to install optional dependencies (critical for Rollup)
include=optional

# Disable audit during install for faster builds
audit=false

# Use exact versions for better reproducibility
save-exact=true

# Prefer offline packages when available
prefer-offline=true
